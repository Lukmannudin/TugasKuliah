
function coba(x)
  select x
  case 0 then 
    disp("Ini satu")
  case 1 then
    disp("Ini dua")
  else
    break
  end
  
   //for i=0:n
      //  y=y+(-1)^i*x^(2*i)/factorial(2*i);
    //end
    //for i=0:1
    //    y=y+fsin(x,i)*h^i/factorial(i);
      //  g=abs(y)-0.14112;
    //end
    //hasil = [g];
endfunction
